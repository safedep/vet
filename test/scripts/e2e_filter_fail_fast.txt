# Run the vet binary using environment variables
exec $E2E_VET_BINARY scan -s --no-banner --github https://github.com/safedep/demo-client-java.git --report-summary=false --filter 'vulns.critical.exists(p, p.id == "GHSA-4wrc-f8pq-fpqp")' --filter-fail

# Check for a failure condition, expect the command to fail
exec exit 1
