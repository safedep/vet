run:
  vetscansvet:
    command: "/bin/bash -c \"vet scan -s --no-banner --lockfiles ${E2E_ROOT}/go.mod --report-summary=false --filter 'pkg.name == \\\"github.com/safedep/dry\\\"'\""
    expectCode: 0
    outputContains:
      - "github.com/safedep/dry"
